<div class="column">
    <div class="card has-background-grey-light">
        <header class="card-header">
            <p class="card-header-title">Derniers événements</p>
        </header>

        <div class="card-content">
            <div class="content">
                <ul>
                    {% for event in events %}
                    <li>{{ event.title }} ({{ event.date | date("d/m/Y H:i") }})</li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </div>
</div>